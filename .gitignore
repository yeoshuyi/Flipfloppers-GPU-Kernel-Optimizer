__pycache__/
*.pyc
.ext_build/
# Throwaway BEFORE-pass copy regenerated by jobs/g4_*_ship_verify*.sbatch via
# `git show <sha>:benchmark.py` -- a copy of a commit git already has.
_before_benchmark.py
# G4.5 SASS investigation scratch: a CuAssembler clone, pip --target installs,
# and cubin/.cuasm build products. Third-party source plus regenerable
# artefacts, not part of this repo. probes/g4_5_sass_roundtrip.py's header
# documents how to recreate it.
.sass/
# G4.6 CUTLASS investigation scratch: a shallow NVIDIA/CUTLASS clone (v4.7.1,
# header-only use -- include/ and tools/util/include/ only, no Profiler and no
# cutlass_library build). Third-party source, not part of this repo.
# probes/g4_6_cutlass_stage0.py's header documents how to recreate it.
.cutlass/

# --- submission packaging (added 2026-08-28) ---
/dist/
*.tar.gz

# local agent config override
.claude/settings.local.json

# profiler / build artefacts (belt-and-suspenders; none tracked today)
*.ncu-rep
*.nsys-rep
*.cubin
*.o
*.so

# misc
.pytest_cache/
.venv/
venv/
.DS_Store

# resume
.RESUME.md
